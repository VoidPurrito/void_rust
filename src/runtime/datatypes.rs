/*use crate::expressions::expression::{AccessModifiers, Types};

pub struct VObject {
    pub visibity: AccessModifiers,
    pub readonly: bool,
    pub name: String,
    pub obj_type: Types,
    pub value: RuntimeValue,
}

union RuntimeValue {
    BoolValue: VBool,
    IntegerValue: VInteger,
    RealValue: VReal,
    StringValue: VString,
}

#[derive(Clone, Copy)]
pub struct VBool {
    pub bool_value: bool,
}

#[derive(Clone, Copy)]
pub struct VInteger {
    pub int_value: i64,
}

#[derive(Clone, Copy)]
pub struct VReal {
    pub real_value: f64,
}

#[derive(Clone)]
pub struct VString {
    pub string_value: String,
}

*/
